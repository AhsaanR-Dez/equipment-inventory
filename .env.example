# Compose reads these to provision the container.
# The seed script builds its connection string from the same values, so there
# is one source of truth. Set DATABASE_URL instead to point at a hosted
# database (that is what a deployed environment would do).
POSTGRES_USER=inventory
POSTGRES_PASSWORD=localdevonly
POSTGRES_DB=inventory
POSTGRES_PORT=5432
